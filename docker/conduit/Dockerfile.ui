# RealWorld Angular SPA — UI for Conduit tests; API URL patched at build time.
# Upstream: https://github.com/realworld-apps/angular-realworld-example-app
FROM node:22-bookworm-slim AS build

WORKDIR /app

RUN apt-get update \
  && apt-get install -y --no-install-recommends git ca-certificates \
  && rm -rf /var/lib/apt/lists/*

ARG CONDUIT_UI_REPO=https://github.com/realworld-apps/angular-realworld-example-app.git
ARG CONDUIT_API_URL=http://127.0.0.1:3000/api

RUN git clone --recurse-submodules --depth 1 "${CONDUIT_UI_REPO}" .

RUN npm install --no-audit --no-fund

RUN sed -i "s|https://api.realworld.show/api|${CONDUIT_API_URL}|g" \
  src/app/core/interceptors/api.interceptor.ts

RUN npx ng build --configuration production

FROM nginx:1.27-alpine AS runtime

COPY docker/conduit/nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist/angular-conduit/browser /usr/share/nginx/html

EXPOSE 80

HEALTHCHECK --interval=5s --timeout=5s --retries=12 \
  CMD wget -qO- http://127.0.0.1/ >/dev/null || exit 1
