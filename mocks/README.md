# Deprecated — moved

> **This folder is a legacy redirect only.** Do not add new files here.

Stub-server implementations moved to **`stub-servers/`**.

| Old path | New path |
|---|---|
| `mocks/` | `stub-servers/` |
| `@mocks/*` | `@stub-servers/*` |

See `stub-servers/README.md`, `docs/INDEX.md`, and `docs/adr/001-stub-servers-vs-contract-tests.md`.
