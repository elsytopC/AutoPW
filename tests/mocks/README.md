# Deprecated — moved

> **This folder is a legacy redirect only.** Do not add new specs here.

Contract tests moved to **`tests/contract/`** (Playwright project `contract`).

| Old path | New path |
|---|---|
| `tests/mocks/` | `tests/contract/` |
| `*.mock.spec.ts` | `*.contract.spec.ts` |
| `--project=mock` | `--project=contract` |

See `docs/INDEX.md`, `docs/testing-layers.md`, and `docs/adr/001-stub-servers-vs-contract-tests.md`.
